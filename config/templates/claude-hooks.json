{
  "$schema": "https://json.schemastore.org/claude-code-settings.json",
  "_comment": "PostToolUse hook for the ai-setup repo. The hook is silent for unrelated edits; for managed config files it runs check + lint and prints a follow-up suggestion to Claude's context.",
  "hooks": {
    "PostToolUse": [
      {
        "matcher": "Edit|Write",
        "hooks": [
          {
            "type": "command",
            "command": "./bin/on-config-edit.sh"
          }
        ]
      }
    ]
  }
}
